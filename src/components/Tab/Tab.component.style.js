import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    tabLabel: {
        alignSelf: 'center',
        fontSize: 10,
        paddingBottom: 6,
    }, 
    tabIcon: { 
        marginBottom: -6,
    },
});

export default styles;